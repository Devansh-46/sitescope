/** @type {import('next').NextConfig} */
const nextConfig = {
  serverExternalPackages: [
    'puppeteer-core',
    '@sparticuz/chromium',
    'lighthouse',
    'chrome-launcher',
  ],
};

module.exports = nextConfig;